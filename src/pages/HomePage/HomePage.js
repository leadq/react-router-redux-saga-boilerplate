import './HomePage.scss'

import React from 'react'
import PropTypes from 'prop-types'

const HomePage = (props) => {
  return <div>This is Private Home Page</div>
}

HomePage.propTypes = {}

export default HomePage
